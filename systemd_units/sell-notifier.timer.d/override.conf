[Timer]
OnCalendar=
# 13:30–13:59 UTC — каждую минуту
OnCalendar=Mon..Fri *-*-* 13:30..59:00 UTC
# 14:00–19:59 UTC — каждую минуту
OnCalendar=Mon..Fri *-*-* 14..19:*:00 UTC
# Опциональный финальный тик в 20:00 UTC
OnCalendar=Mon..Fri *-*-* 20:00:00 UTC
Persistent=true
AccuracySec=15s
# ВАЖНО: Unit здесь НЕ указываем — он берётся из основного sell-notifier.timer
