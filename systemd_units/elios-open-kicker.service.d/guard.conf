[Service]
# Если луп уже стартовал после 18:25 сегодня — НЕ запускать kicker
ExecCondition=/bin/bash -lc '! journalctl -u elios-loop.service -S "today 18:25" -n 1 | grep -q "Started Elios Trading Loop Launcher"'
